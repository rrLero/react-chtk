// @flow

import type {Theme} from '../../with-root/typedef';

const styles = (theme: Theme) => ({
    root: {
        width: 40,
        height: 40
    },
    sm: {
        width: 40,
        height: 40
    },
    md: {
        width: 60,
        height: 60
    },
    lg: {
        width: 80,
        height: 80
    }
});

export default styles;
